<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(@session) %>
      <% user = Helpers.current_user(@sessions) %>
      <h1>Home Page:</h1>
      <h2>Welcome, <%= user.username %>.</h2></h2>
      <p>Your current balance is <%= user.balance %>.</p>
    <% else %>
      <p>You Must <a href="/">Log In</a> to View Your Balance</p>
    <% end %>
    <a href="logout" id="Log Out">Log Out</a>
  </body>
</html>